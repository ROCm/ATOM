name: ATOM Benchmark

on:
  workflow_dispatch:
    inputs:
      model-choice:
        description: 'Select model'
        required: true
        default: 'deepseek'
        type: choice
        options:
          - deepseek
          - gpt-oss

jobs:
  benchmark:
    runs-on: atom-mi355.predownload
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run benchmark
        run: |
