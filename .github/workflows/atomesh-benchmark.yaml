name: K3 prefill IPC import GIL experiment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  prefill-probe:
    runs-on: atomesh-cicd
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v6
      - name: Run single full-model prefill probe
        run: |
          set -euo pipefail
          bash .github/scripts/atomesh/pd_submit.sh \
            --cell-json "$(cat .github/scripts/k3-prefill-probe/cell.json)" \
            --result-dir "prefill-probe-results/${GITHUB_RUN_ATTEMPT}"
      - name: Stop remaining probe and collect evidence
        if: always()
        run: |
          set -euo pipefail
          result_dir="prefill-probe-results/${GITHUB_RUN_ATTEMPT}"
          mkdir -p "${result_dir}"
          cancel_script="${result_dir}/k3-prefill-import-gil-probe.slurm-cancel.sh"
          if [[ -f "${cancel_script}" ]]; then
            timeout --kill-after=5s 90s bash "${cancel_script}" > "${result_dir}/cancel.log" 2>&1 || true
          fi
          marker="${result_dir}/k3-prefill-import-gil-probe.log-root"
          if [[ -f "${marker}" ]]; then
            bash .github/scripts/atomesh/pd_collect_logs.sh "$(cat "${marker}")" "${result_dir}/logs" || true
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k3-prefill-import-gil-${{ github.run_id }}-${{ github.run_attempt }}
          path: prefill-probe-results/${{ github.run_attempt }}/
